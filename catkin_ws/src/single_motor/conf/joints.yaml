joint1:
  controller:
    package: dynamixel_controllers
    module: joint_position_controller
    type: JointPositionController
  joint_name: joint1
  joint_speed: 0.5
  motor:
    id: 1  # the motor id
    init: 512 # joint’s initial position
    min: 0 # max rotation
    max: 1023 # min rotation
